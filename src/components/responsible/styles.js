import styled from 'styled-components'
import {
  Wrapper as ParentWrapper,
  Content as ParentContent,
} from 'pages/student/styles'

export const Wrapper = styled(ParentWrapper)``

export const Content = styled(ParentContent)``
